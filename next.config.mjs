/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "peach.blender.org",
      "upload.wikimedia.org",
      "mango.blender.org",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;